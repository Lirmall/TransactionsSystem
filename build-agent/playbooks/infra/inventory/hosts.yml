all:
  children:
    stand:
      hosts:
        ts-stand-vm:
          ansible_host: 10.80.22.90
          ansible_user: klokov.ai@parmalogica.ru           # юзер для ssh
          ansible_ssh_private_key_file: ~/.ssh/id_rsa  # или через переменную/secure file
